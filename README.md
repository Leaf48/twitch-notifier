# twitch-notifier
Get notified when stream is added

# Requirements
It needs "--no-sandbox"
```typescript
const browser = await puppeteer.launch({
  headless: "new",
  args: ["--no-sandbox"],
});
```
